import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const MainDiv = styled.div `${tw``}`;
const ViewEditToggleHeading = styled.h1 `${tw`font-bold text-4xl`}`;
const InputTag = styled.input `${tw``}`;
const Button = styled.button `${tw`bg-blue-300 text-white`}`;

export {
    MainDiv,
    ViewEditToggleHeading,
    InputTag,
    Button
};
