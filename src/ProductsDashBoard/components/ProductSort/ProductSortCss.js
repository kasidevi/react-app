import tw from 'tailwind.macro';
import styled from '@emotion/styled';

const ProductSortDiv = styled.div `${tw``}`;
const Select = styled.select `${tw``}`;
const Option = styled.option `${tw``}`;

export { ProductSortDiv, Select, Option };
